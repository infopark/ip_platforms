<h1>Listing categories</h1>

<% if is_admin? %>
  <p><%= link_to 'New Category', new_category_path %></p>
<% end %>

<ul>
  <%= render :partial => "categories/category", :collection => @categories.reject { |c| c.parent } %>
</ul>
